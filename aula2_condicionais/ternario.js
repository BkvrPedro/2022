const saldo = 1;
const unidade = (saldo === 1 ? "real" : "reais")

console.log(`O seu saldo é de ${saldo} ${unidade}`)

// SE SALDO FOR 1 REAL "UNIDADE" VAI GUARDAR "REAL", CASO CONTRARIO VAI GUARDAR "REAIS"
// PARA LEMBRAR: QUAL PALAVRA USAR? REAL OU REAIS? (?) ---->  ENTAO = VARIAVEL ? "X" : "Y" (SE VARIAVEL ? "X" SE NÃO (:) "Y")