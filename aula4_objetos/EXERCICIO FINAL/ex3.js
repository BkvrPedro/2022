const usuario = {
    nome: "João",
    idade: 25,
    profissao: "programador",
    cor_preferida: "verde",
}

const { nome, idade, profissao, cor_preferida } = usuario

console.log(`Olá... meu nome é ${nome} e tenho ${idade} anos de idade. Sou ${profissao} e minhar cor preferida é ${cor_preferida}`)